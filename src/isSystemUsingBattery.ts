//import isCharging from "is-charging";
/**
 * uses [`is-charging`](https://github.com/gillstrom/is-charging) library for cross OS support
 */
export const isSystemUsingBattery = async () => {
  // const result = await isCharging();
  return false;
};
